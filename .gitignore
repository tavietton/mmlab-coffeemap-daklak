# Environments
.venv/
venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# OS/editor cruft
.DS_Store
Thumbs.db

# Large raster/shapefile intermediates that are not the final deliverables
*.tif.ovr
*.tif.aux.xml

# Regenerated by notebook 07 (assemble_submission_package): a self-contained
# zip bundle of this repository's results/, duplicated here as a
# build artifact. The authoritative copies are
# results/{tables,figures,supplementary}/.
CoffeePaper1_FinalPackage/
CoffeePaper1_FinalPackage.zip

# --- Public-repository scope: code and pipeline only, no reference-point ---
# --- coordinates, field photos, or large raster outputs.                ---
# Raw inputs: reference-point coordinates (training/validation/candidate
# verification CSVs and KMLs), field photos, and source rasters/statistics.
data/

# Result files that carry per-point lon/lat (traced individually rather than
# excluding all of results/, since most of results/ is safe, citable
# summary output).
results/supplementary/predictions.csv
results/supplementary/S01_invalid_class_or_geometry_rows.csv
results/supplementary/S03_coordinate_duplicates.csv
results/supplementary/S03_exact_duplicates.csv

# Large classified-raster outputs (kept out of git; available on request /
# via the manuscript's data-availability statement rather than git-tracked).
*.tif
*.tif.ovr
*.tif.aux.xml

# GEE_API run outputs carry per-point lon/lat and/or extracted feature values
# (same reference-point-coordinate policy as data/ above); the code that
# produces them is the reproducible artifact, not the CSVs themselves.
GEE_API/test_runs/*.csv
GEE_API/out/
